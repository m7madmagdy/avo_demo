<div class="flex flex-col">
  <%= render Avo::PanelComponent.new(name: 'Custom dashboard', display_breadcrumbs: true) do |c| %>
    <% c.with_tools do %>
      <div class="text-sm italic">This is the panels tools section.</div>
    <% end %>

    <% c.with_body do %>
      <div class="flex flex-col justify-between py-6 min-h-24">
        <div class="px-6 space-y-4">
          <h3>What a nice new tool 👋</h3>

          <p>
            You can edit this file here <code class='p-1 rounded bg-gray-500 text-white text-sm'>app/views/avo/tools/custom_dashboard.html.erb</code>.
          </p>

          <p>
            For this tool we created the <code class='p-1 rounded bg-gray-500 text-white text-sm'>custom_dashboard</code> method in the <code class='p-1 rounded bg-gray-500 text-white text-sm'>Avo::ToolsController</code>. You may delete it or move it to your own controller.
          </p>

          <p>
            A new <code class='p-1 rounded bg-gray-500 text-white text-sm'>Custom dashboard</code> item should have been added to the sidebar. Edit it in <code class='p-1 rounded bg-gray-500 text-white text-sm'>app/views/avo/sidebar/items/_custom_dashboard.html.erb</code>
          </p>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
